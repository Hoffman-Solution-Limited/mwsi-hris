# Frontend dev container (Vite)
FROM node:20-alpine

WORKDIR /usr/src/app

COPY package*.json ./
RUN npm ci

# Copy rest of the app
COPY . .

EXPOSE 8080

# Run Vite dev server listening on all interfaces
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0", "--port", "8080"]
