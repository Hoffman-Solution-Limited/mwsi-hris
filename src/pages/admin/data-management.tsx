import React from "react"
import { Layout } from "../../components/Layout"
import { DataManagementPage } from "@/features/admin/DataManagementPage"

const DataManagement: React.FC = () => (
  <Layout>
    <DataManagementPage />
  </Layout>
)

export default DataManagement
